mindquantum.core.circuit.Circuit.zz(para, obj_qubits, ctrl_qubits=None)

        添加ZZ门。

        参数:
            para (Union[dict, ParameterResolver]): `ZZ`门的参数。
            obj_qubits (Union[int, list[int]]): `ZZ`门的对象量子位。
            ctrl_qubits (Union[int, list[int]]): `ZZ`门的控制量子位。默认值：None。
        