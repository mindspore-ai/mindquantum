.. py:method:: mindquantum.core.operators.QubitOperator.dumps(indent=4)

    将QubitOperator转储到JSON（JavaScript对象表示法）。

    **参数：**

    - **indent** (int) - JSON数组元素和对象成员打印时的缩进。默认值：4。

    **返回：**
    
    JSON(strings)，QubitOperator的JSON字符串。
       