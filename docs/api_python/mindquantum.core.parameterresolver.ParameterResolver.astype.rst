.. py:method:: mindquantum.core.parameterresolver.ParameterResolver.astype(dtype, inplace=False)

    更改此参数解析器的数据类型。

    **参数：**

    - **dtype** (type) – 数据类型。
    - **inplace** (bool) – 是否原地更改类型。默认值：False。

    **返回：**

    ParameterResolver，具有给定数据类型的参数解析器。
