mindquantum.core.parameterresolver.ParameterResolver.para_value

        获取参数值。

        返回：
            list，参数值的列表。

        样例：
            >>> from mindquantum import ParameterResolver
            >>> pr = ParameterResolver({'a': 1, 'b': 2})
            >>> pr.para_value
            [1, 2]
2]
