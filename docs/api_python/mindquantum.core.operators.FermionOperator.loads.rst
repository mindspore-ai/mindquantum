.. py:method:: mindquantum.core.operators.FermionOperator.loads(strs)

    将JSON（JavaScript对象表示法）加载到FermionOperator中。

    **参数：**

    - **strs** (str) - 转储的费米子运算符字符串。

    **返回：**

    FermionOperator，从字符串加载的FermionOperator。
