.. py:method:: mindquantum.core.parameterresolver.ParameterResolver.requires_grad_part(*names)

    设置部分需要渐变的参数。该操作为原位操作。

    **参数：**

    - **names** (tuple[str]) – 需要梯度的参数。

    **返回：**

    ParameterResolver，参数解析器本身。
