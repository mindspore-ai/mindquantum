.. py:method:: mindquantum.core.operators.QubitOperator.loads(strs)

    将JSON（JavaScript对象表示法）加载到QubitOperator中。

    **参数：**

    - **strs** (str) - 转储的量子位运算符字符串。

    **返回：**

    FermionOperator，从字符串加载的QubitOperator。
   