.. py:method:: mindquantum.simulator.Simulator.get_qs(ket=False)

    获取模拟器的当前量子态。

    **参数：**

    - **ket** (bool) – 是否以ket格式返回量子状态。默认值：False。

     **返回：**

    numpy.ndarray，当前量子态。        