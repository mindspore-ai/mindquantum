.. py:method:: mindquantum.core.circuit.Circuit.apply_value(pr)

    将此线路使用输入参数转换成的不含参线路。

    **参数：**

    - **pr** (Union[dict, ParameterResolver]) - 应用到此线路中的参数。

    **返回：**

    Circuit，不含参线路。           