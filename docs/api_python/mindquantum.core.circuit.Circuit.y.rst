.. py:method:: mindquantum.core.circuit.Circuit.y(obj_qubits, ctrl_qubits=None)

    在电路中添加 `Y` 门。

    **参数：**

    - **obj_qubits** (Union[int, list[int]]) - `Y` 门的目标量子比特。
    - **ctrl_qubits** (Union[int, list[int]]) - `Y` 门的控制量子比特。默认值：None。
