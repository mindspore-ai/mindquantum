mindquantum.core.parameterresolver.ParameterResolver.ansatz_parameters

        获取参数，该参数是一个ansatz参数。

        返回：
            set，一个参数的集合。

        >>> from mindquantum.core import ParameterResolver as PR
        >>> a = PR({'a': 1, 'b': 2})
        >>> a.ansatz_parameters
        {'a', 'b'}
'}
