.. py:method:: mindquantum.core.operators.FermionOperator.dumps(indent=4)

    将FermionOperator转储到JSON（JavaScript对象表示法）。

    **参数：**

    - **indent** (int) - JSON数组元素和对象成员打印时的缩进大小。默认值：4。

    **返回：**

    JSON(str)，FermionOperator的JSON字符串。
