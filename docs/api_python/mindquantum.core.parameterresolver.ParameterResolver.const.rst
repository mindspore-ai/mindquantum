mindquantum.core.parameterresolver.ParameterResolver.const

        获取此参数解析器的常量部分。

        返回：
            数字。Number，此参数解析器的常量部分。

        样例：
            >>> from mindquantum.core import ParameterResolver as PR
            >>> pr = PR({'a': 1}, 2.5)
            >>> pr.const
            2.5
.5
