mindquantum.core.circuit.Circuit.xx(para, obj_qubits, ctrl_qubits=None)

        添加XX门。

        参数:
            para (Union[dict, ParameterResolver]): `XX`门的参数。
            obj_qubits (Union[int, list[int]]): `XX`门的对象量子位。
            ctrl_qubits (Union[int, list[int]]): `XX`门的控制量子位。默认值：None。
        