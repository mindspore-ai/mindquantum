.. py:method:: mindquantum.core.parameterresolver.ParameterResolver.no_grad_part(*names)

    设置不需要梯度的部分参数。

    **参数：**

    - **names** (tuple[str]) – 不需要梯度的参数。

    **返回：**

    ParameterResolver，参数解析器本身。
