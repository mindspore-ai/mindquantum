mindquantum.core.parameterresolver.ParameterResolver.items()

        生成所有参数的名称和值的迭代器。

        样例：
            >>> from mindquantum.core import ParameterResolver as PR
            >>> a = PR({'a': 0, 'b': 1})
            >>> list(a.items())
            [('a', 0.0), ('b', 1.0)]
)]
