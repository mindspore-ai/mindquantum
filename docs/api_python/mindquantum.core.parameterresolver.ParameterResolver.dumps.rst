.. py:method:: mindquantum.core.parameterresolver.ParameterResolver.dumps(indent=4)

    将参数解析器转储到JSON（JavaScript对象表示法）

    **参数：**

    - **indent** (int) – 将使用该缩进级别，可读性更高地打印JSON数组元素和对象成员。默认值：4。

    **返回：**

    string(JSON)，参数解析器的JSON。
