mindquantum.core.circuit.Circuit.ry(para, obj_qubits, ctrl_qubits=None)

        添加RY门。

        参数:
            para (Union[dict, ParameterResolver]): `RY`门的参数。
            obj_qubits (Union[int, list[int]]): `RY`门的对象量子位。
            ctrl_qubits (Union[int, list[int]]): `RY`门的控制量子位。默认值：None。
        